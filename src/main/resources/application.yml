spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/drive_me_db?createDatabaseIfNotExist=true
    username: user
    password: password
    driver-class-name: com.mysql.jdbc.Driver
